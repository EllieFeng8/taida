# Taida 數據顯示系統使用者使用說明書

版本：2.0  
適用對象：現場操作人員、設備調機人員、維護人員  
適用系統：Taida 工業冷卻系統即時監控與控制介面

## 1. 系統簡介

Taida 數據顯示系統提供工業冷卻系統的即時數據監控與基本控制操作。使用者可在主畫面查看水路、風路、盤管、泵浦、熱交換與風扇狀態，並依現場操作規範設定出水目標溫度、出水閥、混水閥、混水泵浦、風扇 PID、壓差與各風扇目標值。

系統主畫面標題為「數據顯示系統」，副標題為「即時監控工業冷卻系統狀態」。畫面解析度以 1920 x 1024 介面配置為主。

## 2. 操作前注意事項

1. 操作前請確認現場設備、控制器、網路與安全條件均正常。
2. 所有 SV 設定值輸入後，必須點擊旁邊的「OK」、「套用」、「確認出水設置」、「確認參數」或勾選按鈕，設定才會送出。
3. PV 表示目前值，由系統或控制器回傳；SV 表示設定值，由操作人員輸入。
4. PID、閥門開度、泵浦頻率、壓差與風扇目標值會影響設備運轉，建議由具備權限與調機經驗的人員操作。
5. 乾燥模式或風扇緊急開關啟用時，風扇相關控制會被鎖定，需解除後才能再次操作。

## 3. 畫面區域總覽

主畫面分為以下區域：

- 左側導覽列：底部齒輪圖示可開啟連線設定。
- 標題列：顯示系統名稱，並提供乾燥模式開關與倒數時間。
- 即時監控區：顯示入水、出水、回水、盤管水溫、入風溫濕度與出風溫度。
- 目標溫度區：設定出水目標溫度。
- 出水閥控制區：設定出水閥 PID、修正方向、開度與 P/I/D 參數。
- 混水閥控制區：設定混水閥開度。
- 混水泵浦控制區：設定馬達頻率，並查看水流量與熱交換。
- 風扇控制區：設定風扇 PID、壓差、緊急開關、1 至 9 號風扇與一鍵全開。

## 4. 基本名詞

- PV：目前值，代表系統量測或控制器回傳的實際狀態。
- SV：設定值，代表操作人員準備送出的目標值。
- PID：比例、積分、微分控制參數，用於自動控制閥門或風扇。
- 正向 / 反向修正：控制修正方向。畫面以開關旁文字顯示目前狀態。
- ON / OFF：畫面以開關圖示表示啟用或關閉。
- RPM：風扇轉速單位。
- pa：壓力或壓差單位。

## 5. 啟動與進入系統

1. 啟動 Taida 應用程式。
2. 進入主畫面後，確認監控資料是否正常顯示或更新。
3. 如需調整擷取頻率或模式，點擊左側導覽列底部的設定圖示。
4. 操作控制功能前，請先確認現場設備允許遠端或介面控制。

## 6. 即時監控資料

### 6.1 設備入水

顯示設備入水狀態：

- 溫度：單位為 °C。
- 壓力：單位為 pa。

### 6.2 設備出水

顯示設備出水狀態：

- 溫度：單位為 °C。
- 壓力：單位為 pa。

### 6.3 回水

顯示回水狀態：

- 溫度：單位為 °C。
- 壓力：單位為 pa。

### 6.4 盤管水溫監控

顯示盤管四個量測點的水溫：

- 左入
- 左出
- 右入
- 右出

各欄位單位皆為 °C。

### 6.5 入風溫濕度

顯示入風環境狀態：

- 溫度：單位為 °C。
- 濕度：單位為 %RH。

### 6.6 出風溫度

顯示出風溫度，單位為 °C。

## 7. 乾燥模式

乾燥模式位於畫面上方標題列右側。

操作方式：

1. 點擊「乾燥模式」開關。
2. 開關啟用後，系統進入乾燥模式，並顯示乾燥模式倒數時間。
3. 再次點擊開關可關閉乾燥模式。

限制與注意事項：

- 風扇緊急開關啟用時，乾燥模式無法啟用。
- 乾燥模式啟用時，風扇 PID、風扇修正、目標壓差、單一風扇、一鍵全開與風扇目標值設定皆會被鎖定。
- 若需要操作風扇，請先關閉乾燥模式。
- 倒數時間由系統狀態提供；若倒數不更新，請檢查控制器或通訊狀態。

## 8. 目標溫度設定

目標溫度區顯示目前目標溫度 PV，並提供 SV 輸入欄位。

操作方式：

1. 在目標溫度 SV 欄位輸入大於 0 的數值。
2. 點擊旁邊的「OK」按鈕。
3. 系統送出新的出水目標溫度。

注意事項：

- 輸入值必須大於 0，否則不會套用。
- 輸入後若未點擊 OK，設定不會送出。

## 9. 出水閥控制

出水閥控制區提供 PID 開關、修正方向、開度與 P/I/D 參數設定。

### 9.1 PID 開關

操作方式：

1. 點擊出水閥控制區的 PID 開關。
2. 畫面顯示「PID ON」表示啟用；顯示「PID OFF」表示關閉。

### 9.2 修正方向

操作方式：

1. 點擊修正開關。
2. 開關旁文字會在「正向」與「反向」之間切換。

注意事項：

- 修正方向會影響控制邏輯，請依現場調機規範操作。

### 9.3 設定開度與 PID 參數

畫面欄位：

- 開度 PV：目前出水閥開度。
- 開度 SV：欲設定的出水閥開度。
- P/I/D PV：目前 PID 參數。
- P/I/D SV：欲設定的 PID 參數。

操作方式：

1. 在「開度」SV 欄位輸入目標開度。
2. 在 P、I、D 的 SV 欄位輸入控制參數。
3. 點擊「確認出水設置」。
4. 系統送出出水閥開度與 P/I/D 參數。

## 10. 混水閥控制

混水閥控制區用於設定混水閥開度。

畫面欄位：

- 開度 PV：目前混水閥開度。
- 開度 SV：欲設定的混水閥開度。

操作方式：

1. 在開度 SV 欄位輸入目標開度。
2. 點擊「確認參數」。
3. 系統送出新的混水閥開度。

## 11. 混水泵浦控制

混水泵浦控制區提供馬達頻率開關、頻率設定、水流量與熱交換顯示。

### 11.1 馬達頻率開關

操作方式：

1. 點擊馬達頻率控制開關。
2. 系統切換馬達頻率控制狀態。

### 11.2 設定馬達頻率

畫面欄位：

- 頻率 PV：目前馬達頻率，單位為 Hz。
- 頻率 SV：欲設定的馬達頻率，單位為 Hz。

操作方式：

1. 在「頻率」SV 欄位輸入目標頻率。
2. 點擊「套用」。
3. 系統送出新的馬達頻率。

### 11.3 水流量與熱交換

畫面顯示：

- 當前水流量：單位為 l/min。
- 當前熱交換：單位為 kW。

## 12. 風扇控制

風扇控制區包含風扇 PID、風扇修正、壓差、目標壓差、緊急開關、1 至 9 號風扇控制，以及「一鍵開啟全部」。

重要限制：

- 乾燥模式啟用時，所有風扇操作皆會被鎖定。
- 風扇緊急開關啟用時，所有風扇操作皆會被鎖定。
- 若風扇控制無法操作，請先確認乾燥模式與緊急開關是否已解除。

### 12.1 風扇 PID 啟動監控

操作方式：

1. 點擊「PID 啟動監控」開關。
2. 系統切換風扇 PID 監控狀態。

### 12.2 風扇修正方向

操作方式：

1. 點擊風扇修正開關。
2. 開關旁文字會在「正向」與「反向」之間切換。

注意事項：

- 修正方向會影響風扇控制邏輯，請依現場調機規範操作。

### 12.3 設定風扇 PID 參數

畫面欄位：

- P/I/D PV：目前風扇 PID 參數。
- P/I/D SV：欲設定的風扇 PID 參數。

操作方式：

1. 在 P、I、D 的 SV 欄位輸入目標參數。
2. 點擊風扇 PID 區的「套用」。
3. 系統送出風扇 PID 參數。

### 12.4 壓差與目標壓差

畫面欄位：

- 壓差：目前壓差，單位為 pa。
- 目標壓差：欲設定的壓差，單位為 pa。

操作方式：

1. 在「目標壓差」欄位輸入大於 0 的數值。
2. 點擊旁邊的「套用」。
3. 系統送出新的目標壓差。

注意事項：

- 輸入值必須大於 0，否則不會套用。

### 12.5 緊急開關

操作方式：

1. 點擊「緊急開關」。
2. 啟用後，系統會關閉乾燥模式、關閉 1 至 9 號風扇，並取消一鍵全開。
3. 緊急開關啟用期間，風扇相關控制會被鎖定。
4. 再次點擊「緊急開關」可解除緊急狀態。

注意事項：

- 緊急開關會影響全部風扇運轉，請僅在需要立即停止或保護設備時使用。
- 解除緊急狀態後，請依現場狀況重新啟動需要運轉的風扇。

### 12.6 單一風扇控制

每個風扇區塊包含：

- 風扇編號：風扇 1 至風扇 9。
- 開關：切換該風扇 ON/OFF。
- PV 百分比：目前輸出百分比。
- PV RPM：目前轉速。
- SV：欲設定的目標值。
- 勾選按鈕：送出該風扇 SV 設定。

操作方式：

1. 找到欲設定的風扇，例如「風扇 1」。
2. 如需直接啟停，點擊該風扇右上方開關。
3. 如需設定目標值，在 SV 欄位輸入 1 至 100 的整數。
4. 點擊勾選按鈕送出設定。

系統行為：

- 單一風扇送出有效 SV 後，該風扇會自動切換為 ON。
- 若原本處於一鍵全開狀態，操作單一風扇或送出單一風扇 SV 會取消一鍵全開狀態。
- 若輸入值小於 0 或大於 100，欄位文字會變紅，表示該數值無效。

### 12.7 一鍵開啟全部

操作方式：

1. 點擊「一鍵開啟全部」開關。
2. 開啟後，系統會將 1 至 9 號風扇全部切換為 ON。
3. 關閉後，系統會將 1 至 9 號風扇全部切換為 OFF。

### 12.8 設定全部風扇目標值

操作方式：

1. 在「一鍵開啟全部」區塊的 SV 欄位輸入 1 至 100 的數值。
2. 點擊勾選按鈕。
3. 系統會將 1 至 9 號風扇目標值全部設定為相同數值。

系統行為：

- 若一鍵全開尚未啟用，送出全部風扇目標值後，系統會自動啟用一鍵全開並開啟全部風扇。
- 若輸入值小於 0 或大於 100，欄位文字會變紅，表示該數值無效。

## 13. 連線設定

點擊左側導覽列底部的設定圖示，可開啟「連線設定」視窗。

連線設定包含：

- IP 地址：顯示目前連線 IP，畫面中為顯示欄位，不提供直接編輯。
- 擷取頻率：設定資料擷取頻率。
- 模式選擇：可選擇「連動」或「單機」。
- 套用：儲存擷取頻率並關閉視窗。
- 取消：不套用變更並關閉視窗。

操作方式：

1. 點擊左側導覽列底部的設定圖示。
2. 檢查 IP 地址是否正確。
3. 在「擷取頻率」欄位輸入數值。
4. 選擇「連動」或「單機」模式。
5. 點擊「套用」完成設定，或點擊「取消」離開。

模式說明：

- 連動：模式代碼為 0。
- 單機：模式代碼為 1。

注意事項：

- 點選「連動」或「單機」時，模式會立即更新為對應狀態。
- 點擊「套用」時會送出擷取頻率並關閉視窗。
- 點擊「取消」會直接關閉視窗，不會送出擷取頻率變更。

## 14. 常見操作流程

### 14.1 日常監控

1. 啟動系統並進入主畫面。
2. 確認設備入水、設備出水、回水、盤管水溫、入風溫濕度與出風溫度是否正常。
3. 確認水流量、熱交換與壓差是否符合現場要求。
4. 確認各風扇 PV 百分比與 RPM 是否符合預期。

### 14.2 調整控制參數

1. 確認設備安全條件與操作權限。
2. 找到欲設定的區塊。
3. 在 SV 欄位輸入目標值。
4. 點擊對應的 OK、套用、確認或勾選按鈕。
5. 觀察 PV 是否依預期變化。

### 14.3 風扇緊急處理

1. 需要立即停止全部風扇時，點擊「緊急開關」。
2. 確認 1 至 9 號風扇已關閉，且一鍵全開已取消。
3. 排除現場異常。
4. 再次點擊「緊急開關」解除緊急狀態。
5. 依現場需求重新啟動風扇或重新設定目標值。

## 15. 常見問題排查

### 15.1 數據沒有更新

處理方式：

1. 開啟連線設定，確認 IP 地址。
2. 確認擷取頻率是否合理。
3. 確認模式選擇是否符合現場連線方式。
4. 檢查控制器、網路與通訊服務是否正常。

### 15.2 輸入數值後沒有生效

處理方式：

1. 確認是否點擊對應的 OK、套用、確認或勾選按鈕。
2. 確認輸入值是否符合欄位限制。
3. 確認相關控制開關是否已啟用。
4. 若為風扇相關操作，確認乾燥模式與緊急開關是否已解除。
5. 檢查現場控制器是否接受該命令。

### 15.3 風扇欄位無法操作

可能原因：

- 乾燥模式正在啟用。
- 風扇緊急開關正在啟用。

處理方式：

1. 若乾燥模式啟用，先關閉乾燥模式。
2. 若緊急開關啟用，確認現場安全後解除緊急開關。
3. 重新操作風扇開關、SV 設定或 PID 設定。

### 15.4 風扇 SV 欄位文字變紅

原因：

- 輸入值小於 0 或大於 100。

處理方式：

1. 將 SV 改為 1 至 100 的數值。
2. 再次點擊勾選按鈕。

### 15.5 乾燥模式無法啟用

可能原因：

- 風扇緊急開關目前已啟用。

處理方式：

1. 確認現場安全。
2. 解除風扇緊急開關。
3. 再次啟用乾燥模式。

## 16. 安全提醒

- 不確定設備狀態時，請勿任意修改 PID、閥門開度、馬達頻率、壓差或風扇目標值。
- 使用緊急開關後，需確認現場狀態再恢復風扇運轉。
- 若畫面資料長時間停留在 0 或不更新，請優先檢查通訊與控制器狀態。
- 本系統提供介面操作功能，實際設備保護、連鎖條件與允許範圍仍應以現場控制器與安全規範為準。
